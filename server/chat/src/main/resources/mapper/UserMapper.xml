<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lec.projects.chat.mapper.UserMapper">
  <resultMap id="BaseResultMap" type="lec.projects.chat.entity.User">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="username" jdbcType="CHAR" property="username" />
    <result column="email" jdbcType="CHAR" property="email" />
    <result column="password" jdbcType="CHAR" property="password" />
    <result column="avatar" jdbcType="VARCHAR" property="avatar" />
    <result column="phone" jdbcType="CHAR" property="phone" />
  </resultMap>
  <sql id="Base_Column_List">
    id, username, email, password, avatar, phone
  </sql>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from tb_user
    where id = #{id,jdbcType=INTEGER}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from tb_user
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <update id="updateByPrimaryKeySelective" parameterType="lec.projects.chat.entity.User">
    update tb_user
    <set>
      <if test="username != null">
        username = #{username,jdbcType=CHAR},
      </if>
      <if test="email != null">
        email = #{email,jdbcType=CHAR},
      </if>
      <if test="password != null">
        password = #{password,jdbcType=CHAR},
      </if>
      <if test="avatar != null">
        avatar = #{avatar,jdbcType=VARCHAR},
      </if>
      <if test="phone != null">
        phone = #{phone,jdbcType=CHAR},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>
  <update id="updateByPrimaryKey" parameterType="lec.projects.chat.entity.User">
    update tb_user
    set username = #{username,jdbcType=CHAR},
      email = #{email,jdbcType=CHAR},
      password = #{password,jdbcType=CHAR},
      avatar = #{avatar,jdbcType=VARCHAR},
      phone = #{phone,jdbcType=CHAR}
    where id = #{id,jdbcType=INTEGER}
  </update>
  <select id="selectByUsernameAndPassword" parameterType="lec.projects.chat.form.LoginForm" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from tb_user
    where username = #{username} and password = #{password}
  </select>
  <insert id="insertUser" useGeneratedKeys="true" keyProperty="id" parameterType="lec.projects.chat.form.LoginForm" >
    insert into tb_user(username,password) values(#{username},#{password})
  </insert>

  <select id="selectUsername" parameterType="java.lang.String" resultType="java.lang.Integer">
    select count(1)
    from tb_user
    where username = #{username}
  </select>

  <select id="getAvatarsByNames" parameterType="java.util.Set" resultMap="BaseResultMap">
    select username, avatar
    from tb_user
    where
    <if test="usernames!= null and usernames.size() &gt; 0">
      username in
      <foreach close=")" collection="usernames" item="value" open="(" separator=",">
        #{value}
      </foreach>
    </if>
  </select>

  <update id="setAvatarById">
    update tb_user
    set avatar = #{newAvatarPath}
    where id = #{id}
  </update>

  <select id="getAvatarsById" parameterType="java.lang.String" resultType="java.lang.String">
    select avatar
    from tb_user
    where id = #{id}
  </select>

</mapper>